package game;

import java.awt.Frame;

public class Program {
	public static void main(String[] args) {
		Frame frame = new GameFrame();
		
		/* API를 사용
		 (API) 기존의 Frame을 GameFrame으로 새로 고침, 기존의 Canvas를 GameCanvas 로 고쳐 사용> is상속
		 GameFrame 안에 GameCanvas 붙여쓰기 > has상속
		 */
	}
}
